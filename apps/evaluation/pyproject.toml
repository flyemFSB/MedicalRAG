[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "medicalrag-evaluation"
version = "0.1.0"
description = "MedicalRAG isolated evaluation runner (deterministic retrieval metrics + optional RAGAS)"
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/medicalrag_evaluation"]
