{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "!dist/**/*.map"]
    },
    "lint": {},
    "typecheck": {},
    "test:unit": {
      "dependsOn": ["^build"]
    }
  }
}
